// import the Media class:
const Media = require(Media.js)
// create your Book class:
class Book{
    constructor(author,numPages,rating){
    this.author = author;
    this.numPages = numPages;
    this.rating= rating;
    }

    summary(){
        return `Title: ${title}, Author: ${author}, Page Count: ${numPages}, Genre: ${genre}, Rating ${rating}`
    }
    static highestRating(books){
    for (let i = 0; i < books.length; i++) {
    let winner = {}
    if(books[5] > winner[5]){
        winner = book
    }

}
    
}
   
}
// don't change below
module.exports = Book;
